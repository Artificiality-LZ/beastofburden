# MineColonies 挂钩

> 开发对照版本 **1.1.873**。`mods.toml` 范围 `[1.1.873,)`（873 及以上，无上限）。更新年份的内部 API 可能仍会变，不要按 1214 文档想当然。

## 本地源码目录（只读，禁止改）

| 目录 | 上游标签 | 用途 |
|------|----------|------|
| `minecolonies-1.20.1-1.1.873/` | `v1.20.1-1.1.873` | **主对照**，与本模组 Curse 依赖一致 |
| `minecolonies-1.20.1-1.1.1214/` | `v1.20.1-1.1.1214` | 较新 1.20.1 对照（文档里的「1214」；**没有** `v1.20.1-1.1.1314`） |
| `minecolonies-release-1.20/` | （旧无版本复制，仍是 `coremod`） | **可删**；请改用上面带版本号的目录 |

两份带版本号的树都是完整 zip 展开（含蓝图），目录内有 `SOURCE_TAG.txt`。均已 gitignore。

编译仍走 CurseMaven `6444411`，不是这些目录。

## 稳定依赖（Gradle）

MineColonies curse 构件 `6444411`；Structurize / BlockUI / Domum Ornamentum / Multi-Piston 版本见 `AGENTS.md`。加载顺序 `minecolonies` AFTER。

---

## 873 树里有什么（主对照）

规模约：API + 实现合计 **~2006** 个 `.java`；实现根包已是 **`com.minecolonies.core`**（不是 `coremod`）。农田在 **`colony.buildingextensions`**。

| 区 | 路径 | 说明 |
|----|------|------|
| API | `src/main/java/com/minecolonies/api/`（873 树；旧文档曾写 `src/api/java`） | 对外接口、注册表、常量 |
| 实现 | `src/main/java/com/minecolonies/core/` | 殖民地逻辑、AI、建筑、网络、GUI |
| API 实现桥 | `src/main/java/com/minecolonies/apiimp/` | `*Initializer` |
| 蓝图 | `src/main/resources/blueprints/minecolonies/` | Structurize 风格包 |

### 顶层职责（按包）

**`com.minecolonies.api`**

| 子包 | 干什么 | 本模组是否常用 |
|------|--------|----------------|
| `colony` | 殖民地、市民、建筑、工单、权限、请求系统 | **是** |
| `colony.buildings` | `ModBuildings`、`IBuilding`、模块接口、`BuildingEntry` | **是** |
| `colony.jobs` / `jobs.registry` | `JobEntry`、`IJob` | **是** |
| `colony.requestsystem` | 请求、解析器、token、可配送物 | **是** |
| `colony.workorders` | `IWorkOrder`、`IServerWorkOrder`、`WorkOrderType` | **是** |
| `colony.buildingextensions` | 农田等建筑扩展（873+） | **是**（registry API） |
| `colony.managers.interfaces` | `IRegisteredStructureManager` | **是** |
| `entity.ai` / `entity.citizen` | AI 状态、技能、模型 | **是** |
| `blocks` / `util` / `compatibility.newstruct` | hut、稻草人、`BlueprintMapping` | **是** |

**`com.minecolonies.core`（编译期直依赖，无对等 API 基类）**

| 子包 | 干什么 | 本模组是否常用 |
|------|--------|----------------|
| `colony.jobs` / `entity.ai.workers` | `AbstractJob`、`AbstractAISkeleton` | **是**（职业 / AI 基类） |
| `colony.buildingextensions` / `buildings.workerbuildings` | `FarmField`、`BuildingFarmer.FarmerFieldsModule` | **是**（农田 seed / assign） |
| `entity.ai.workers.util` | `ConstructionTapeHelper` | **是**（施工胶带） |
| `client.gui` / `jobs.views` / `moduleviews` | GUI / JobView / ModuleView | **是** |
| `Network` | `MarkBuildingDirtyMessage` | 市政厅页脏同步 |

### 873 vs 1214（快速对照）

两份都是 `core` + `buildingextensions`。1214 更新、类更多（约 2079 Java）；升版本前用 1214 树 diff 敏感 API（`FarmField` / `FarmerFieldsModule` / 胶带 / GUI 基类）。

旧复制 `minecolonies-release-1.20` 仍是 `coremod` + `colony.fields`，**不要**再当 873 真相。

**已确认的破坏性改名（873 → 1214+，1240 同批）：**

| 873 | 1214+ | 本模组策略 |
|-----|-------|-----------|
| `IColony.getBuildingManager()` | `getServerBuildingManager()`（另有 `getCommonBuildingManager()`） | `MineColoniesCompat` 用 `MethodHandles` 按序探测；**禁止**字节码直调旧名 |
| `core.client.gui.modules.SpecialAssignmentModuleWindow(IBuildingView, String)` | `...gui.modules.building.SpecialAssignmentModuleWindow(IAssignmentModuleView, ResourceLocation)` | 市政厅页改为继承 BlockUI `BOWindow`，不继承 MC 窗口基类 |
| `IBuildingModuleView.getDesc()` → `String` | → `Component` | 仍编 873 签名；1214+ 上页签标题可能不准，但不崩 |
| 市政厅主界面侧栏 | `AbstractWindowTownHall.shouldRenderDefaultSidebar()` 返回 false | 牛马页签可能被藏；可选 mixin 另议，勿硬钉进 873 |

**兼容原则：** Gradle / Curse 编译目标保持 **1.1.873**；`mods.toml` 范围仍 `[1.1.873,)`。对已确认的硬崩溃做运行时探测或脱离搬家的父类，不宣称 1240 全功能等价。

---

## 使用的内部面

| 能力 | API / 类 | 本模组用法 |
|------|----------|------------|
| Job 注册 | `DeferredRegister<JobEntry>` on `minecolonies:jobs` | `BeastofburdenJobs` |
| 建筑模块 | `BuildingEntry.ModuleProducer`、`IAssignsJob` 等 | 挂 townhall |
| 建筑枚举 | `IRegisteredStructureManager`（`getTownHall` / `getBuildings` / `getWareHouses`） | `ColonyBuildings` ← `MineColoniesCompat` |
| 市民 AI | `AbstractAISkeleton`、`AIWorkerState` | 补给状态机 |
| 请求系统 | `IPlayerRequestResolver`、`IRetryingRequestResolver`、`overruleRequest` | 卡住检测与履约 |
| 仓库 | `hasMatchingItemStackInWarehouse`、rack 提取 | 供应检查 |
| 工单 | `IServerWorkOrder.isValid`、`requestUpgrade` | 规划忙碌判定 / 产物 |
| 朝向 | `ISchematicProvider.getRotation` | `BuildingFootprint` |
| 蓝图 | Structurize `Blueprint` / `StructurePacks`、`BlueprintMapping` | 选址与粘贴 |
| 农田 | `BuildingExtensionRegistries.farmField.produceExtension`、`ModBlocks.blockScarecrow`、core `FarmField` / `FarmerFieldsModule.assignExtension` | `ColonyFieldSupport` |
| 施工胶带 | core `ConstructionTapeHelper.placeConstructionTape(Tuple, IColony)` | `ConstructionTapeSupport` |
| 自动雇佣 | API：`canAssignCitizens` / `HiringMode` / `isManualHiring`（内联，不调 `BuildingUtils`） | `TownHallBeastofburdenModule` |
| GUI | BlockUI `BOWindow` + `WindowHireWorker` / `RecallCitizenMessage` / `MarkBuildingDirtyMessage` | 市政厅牛马页（不继承 `SpecialAssignmentModuleWindow`） |
| 权限 | `Action.MANAGE_HUTS` | 规划相关 C2S |

### 不可替换的 core 基类（升级 checklist）

升 MineColonies 版本时优先 diff 这些编译依赖（改名/签名变更会**直接编译失败**，或在更高版本运行时 `NoSuchMethodError` / `ClassNotFoundException`）：

- `AbstractJob`、`AbstractAISkeleton`、`DefaultJobView`
- `WorkerBuildingModuleView`、`WindowHireWorker`（市政厅页已不依赖 `SpecialAssignmentModuleWindow`）
- `FarmField`、`BuildingFarmer.FarmerFieldsModule`、`ConstructionTapeHelper`
- `MarkBuildingDirtyMessage`（`com.minecolonies.core.Network`）
- `IColony` 建筑管理器访问（经 `MineColoniesCompat`，勿直调 `getBuildingManager`）

### 已取消的反射表

下列字符串 FQCN 反射已删除，改为 API、core 直 import，或 MethodHandles 兼容层：

| 原用途 | 现实现 |
|--------|--------|
| `getTownHall` / `getBuildings` / `getWareHouses` | `IRegisteredStructureManager`（经 `MineColoniesCompat` 取 manager） |
| `FarmField.create` / scarecrow / extension type | registry + `ModBlocks` + `FarmField` |
| `FarmerFieldsModule.assignExtension` | core `instanceof` + `assignExtension` |
| `ConstructionTapeHelper` | 直调 |
| 工单 `isValid` / 建筑 `getRotation` | `IServerWorkOrder` / `getRotation()` |

---

## 已知脆弱点

1. **Core 编译依赖**：农田模块、胶带、Job/AI 基类仍钉在 `com.minecolonies.core`；升版本以编译错误为准。GUI 市政厅页已脱离会搬家的 MC 窗口基类。
2. 模块侧栏：873 原生显示；1214+ 市政厅 `shouldRenderDefaultSidebar() == false`，牛马页签可能不可见（本模组暂无 mixin）。
3. `AbstractBlockHut.onBlockPlacedByBuildTool`、level 0 是规划落 hut 的契约。普通建筑随后 `requestUpgrade`；失败路径会回滚方块/注销建筑。**第一座建造者**先只落 hut，雇到人后再 `requestUpgrade`，避免 MineColonies 的「需要建造者」拒绝把方块回滚掉。
4. Job 模型必须 `SETTLER_ID`，`CITIZEN_ID` 会裁切。

升版本 checklist：对照本页 API / core 清单 → `compileJava` → 跑 `runClient` 开市政厅页 → 冷启动第一座建造者 → 卡住请求生成配送 → 固定式走完第 1–5 步（含农田）。在 1214+ 整合包上再验：建殖民地无 `NoSuchMethodError`；牛马页类可加载。

### 建议阅读顺序（在 `minecolonies-1.20.1-1.1.873/`）

1. `api/.../IMinecoloniesAPI.java`
2. `api/.../ModBuildings.java`、`IRegisteredStructureManager.java`
3. `api/.../buildings/modules/*`、`buildingextensions/**`
4. `api/.../requestsystem/**`、`workorders/IServerWorkOrder.java`
5. `core/.../AbstractJob`、`AbstractAISkeleton`、`BuildingTownHall`
6. `core/.../buildingextensions/FarmField`、`BuildingFarmer`、胶带 helper
